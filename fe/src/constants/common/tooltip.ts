/**
 * Single app-wide hover-to-tooltip delay. Every tooltip surface (Base UI
 * tooltips, DynamicTooltip groups, icon-button touch long-press) must derive
 * its open delay from this constant so the timing stays unified.
 */
export const TOOLTIP_HOVER_OPEN_DELAY_MS = 1000;
