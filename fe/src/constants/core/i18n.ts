import type { Locale } from '@/@types/core/integrations/i18n/config';

export const SUPPORTED_LANGUAGES: Locale[] = ['en', 'ar'],
  RTL_LANGUAGES: Locale[] = ['ar'],
  LANGUAGE_STORAGE_KEY = 'diwan:language';
